var vector = [];
for (let i = 0; i < 20; i++) {
    vector[i] = Math.round((Math.random()) * 100);
}
var par = vector[i],
    impar = vector[i]
if (par == 0) {
    console.log('par' + par);
} else if (impar != 0) {
    console.log('impar' + impar);
}